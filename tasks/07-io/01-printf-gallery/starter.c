#include <stdio.h>

int main(void)
{
    /* TODO: print the header, the four data rows and the TOTAL line.
     * Useful pieces: %-12s (left-justified string), %6d, %10.2f,
     * %12.2f, %6.1f and %% for a literal percent sign. */
    printf("TODO\n");
    return 0;
}
