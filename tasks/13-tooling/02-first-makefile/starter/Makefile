# TODO: replace this placeholder with a real Makefile — see prompt.md.
#
# Required: a default target that builds `calc` from main.o and ops.o,
# a CFLAGS variable, a %.o: %.c pattern rule (depending on ops.h too),
# and a .PHONY clean target that removes calc and the .o files.

all:
	@echo "error: the Makefile has not been written yet" >&2
	@exit 1
