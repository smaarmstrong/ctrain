#include <stddef.h>

long find_first(const int *a, size_t n, int key)
{
    /* TODO: binary search biased left — keep going after a hit. */
    (void)a;
    (void)n;
    (void)key;
    return -1;
}

long find_last(const int *a, size_t n, int key)
{
    /* TODO: binary search biased right. */
    (void)a;
    (void)n;
    (void)key;
    return -1;
}
