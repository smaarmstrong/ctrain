#include <stddef.h>

long sum_range(const int *begin, const int *end)
{
    /* TODO: walk from begin up to (not including) end. */
    (void)begin;
    (void)end;
    return 0;
}

const int *find_first(const int *begin, const int *end, int x)
{
    /* TODO: return a pointer to the first match, or end if none. */
    (void)begin;
    (void)x;
    return end;
}

size_t count_above(const int *begin, const int *end, int x)
{
    /* TODO */
    (void)begin;
    (void)end;
    (void)x;
    return 0;
}
