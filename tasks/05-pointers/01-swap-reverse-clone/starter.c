#include <stddef.h>
#include <stdlib.h>

void swap(int *a, int *b)
{
    /* TODO */
    (void)a;
    (void)b;
}

void reverse(int *a, size_t n)
{
    /* TODO */
    (void)a;
    (void)n;
}

int *clone_ints(const int *src, size_t n)
{
    /* TODO: allocate, copy, return — the caller frees. */
    (void)src;
    (void)n;
    return NULL;
}
