#include <stdio.h>

int main(void)
{
    int a = 1234;
    int b = 56;

    /* TODO: six lines — values, sum/diff, quot/rem, ratio, pct, boxed.
     * Compute everything from a and b; mind the widths and precisions. */
    printf("a=%d b=%d\n", a, b);
    return 0;
}
