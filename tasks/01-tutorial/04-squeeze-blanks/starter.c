#include <stdio.h>

int main(void)
{
    /* TODO: loop on getchar() until EOF, echo with putchar(),
     * but let only the first space of each run through.
     * getchar() returns an int — store it in one. */
    return 0;
}
