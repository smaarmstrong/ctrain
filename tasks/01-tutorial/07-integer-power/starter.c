/* Implement int_pow here. Do NOT define main — the grader provides it. */

long int_pow(int base, int exp)
{
    /* TODO: multiply base into an accumulator exp times. */
    (void)base;
    (void)exp;
    return 0;
}
