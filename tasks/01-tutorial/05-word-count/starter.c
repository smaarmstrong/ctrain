#include <stdio.h>

int main(void)
{
    /* TODO: one pass over getchar() — count characters, newlines,
     * and word starts (a non-blank after blank/start-of-input). */
    printf("0 0 0\n");
    return 0;
}
