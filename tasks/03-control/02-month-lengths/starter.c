int is_leap(int year)
{
    /* TODO: divisible by 4, except centuries, except every 4th century. */
    (void)year;
    return 0;
}

int days_in_month(int month, int year)
{
    /* TODO: a switch with grouped, deliberately falling-through cases. */
    (void)month;
    (void)year;
    return 30;
}
