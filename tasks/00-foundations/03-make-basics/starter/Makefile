# TODO: one rule.
#
#   target: dependencies
#   <TAB>   recipe
#
# Target `greet`, built from main.c, recipe
# `cc -Wall -Werror -o greet main.c` — and the recipe line must start
# with a real Tab character (spaces = "missing separator"). See prompt.md.
