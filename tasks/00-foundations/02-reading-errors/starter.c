/* A tiny receipt printer. It does not compile — three classic mistakes.
 * Fix the FIRST reported error, recompile, repeat. See prompt.md. */
#include <stdio.h>

int main(void)
{
    int apples = 3;
    int price = 25;              /* pence each */
    int total = apples * price
    printf("%d apples\n", appels);
    printf("total: %s pence\n", total);
    return 0;
}
