#include <stdio.h>

int main(void)
{
    int apples = 3;
    int price = 25;              /* pence each */
    int total = apples * price;
    printf("%d apples\n", apples);
    printf("total: %d pence\n", total);
    return 0;
}
