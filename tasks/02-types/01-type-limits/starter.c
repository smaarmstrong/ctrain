#include <stdio.h>
/* You will need <limits.h> and <float.h>. */

int main(void)
{
    /* TODO: twelve lines, "NAME value", values from the standard macros.
     * Pick the conversion specifier that matches each type:
     *   int -> %d   unsigned -> %u   long long -> %lld
     *   size_t -> %zu   double -> %g
     */
    printf("TODO\n");
    return 0;
}
