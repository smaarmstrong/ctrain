#include <stdint.h>

int popcount32(uint32_t x)
{
    /* TODO: count the 1-bits. */
    (void)x;
    return 0;
}

int trailing_zeros32(uint32_t x)
{
    /* TODO: zeros below the lowest set bit; 32 when x is 0. */
    (void)x;
    return 0;
}

uint32_t rotl32(uint32_t x, unsigned k)
{
    /* TODO: rotate left by k % 32 — without ever shifting by 32. */
    (void)k;
    return x;
}

uint32_t reverse_bits32(uint32_t x)
{
    /* TODO: mirror all 32 bits. */
    return x;
}
